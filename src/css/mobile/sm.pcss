/* Small (sm) */
@media (min-width: 640px) {
  .logo .mobile {
    @apply hidden;
  }

  nav {
    @apply flex flex-row;
  }

  .nav-button {
    @apply w-1/4;
  }
  .index-link {
    @apply w-64 mx-auto;
  }
}
